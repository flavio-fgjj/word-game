export enum ScoreEnum {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  synonyms_already_seen = 1.5,
  // eslint-disable-next-line @typescript-eslint/naming-convention
  meaning_already_seen = 1,
  // eslint-disable-next-line @typescript-eslint/naming-convention
  phrase_already_seen = 2
}
